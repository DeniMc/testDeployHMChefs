{
  "name": "frontend",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "app.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "copyfiles -u 1 src/css/*.css dist/css",
    "build:js": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "resolutions": {
    "cssnano-preset-simple": "3.0.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
